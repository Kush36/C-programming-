#include<stdio.h>
int main()
{
  int n,sum,count;
  printf("Enter a value of number n ");
  scanf("%d",&n);
  sum=0;
  while(n!=0){
      if(n%2;==0)
      n=n/10;
      sum=sum+count;
  }
   printf("sum of even digits is %d ",sum);
   return 0;
}